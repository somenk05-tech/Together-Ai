-- The Personalized Store drew every product under every shortlisted supplement.
-- The kit is one pack per supplement inside a number the citizen sets, ₹ a month,
-- saved here so every screen reads the same one. Null = never set.
ALTER TABLE "FitnessProfile" ADD COLUMN "supplementBudgetInr" INTEGER;
