# dependencies
node_modules/
# build artifacts
dist/
coverage/
*.tsbuildinfo
.vite/
# environment secrets (templates like .env.example / .env.production stay tracked)
.env
.env.local
# OS noise
.DS_Store

# Scratch: this sandbox cannot delete files, only move them here.
_to_delete/
_claude_scratch/
pet-district/
*.patch
