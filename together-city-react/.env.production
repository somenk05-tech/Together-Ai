# Production frontend config — the live Railway backend.
# These are baked into the Vite build; override in the Vercel dashboard if the URL ever changes.
VITE_API_URL=https://together-ai-production.up.railway.app/api
VITE_SOCKET_URL=https://together-ai-production.up.railway.app
