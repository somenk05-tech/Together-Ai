# Production frontend config — point these at your deployed backend.
# Set the SAME values in your host's dashboard (Netlify/Vercel/Cloudflare env vars).
VITE_API_URL=https://YOUR-BACKEND-DOMAIN/api
VITE_SOCKET_URL=https://YOUR-BACKEND-DOMAIN
